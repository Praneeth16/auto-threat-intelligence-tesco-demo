"""Streaming intelligence: replay, enrichment, scoring, report extraction.

The scoring engine is pure pandas so the hero-finding rank can be proven
locally before it runs inside the Lakeflow pipeline.
"""
